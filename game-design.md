# 황금별을 잡아라 — 웹 게임 설계서

## 문서 목적

이 문서는 `game-plan.md`의 기획을 HTML, CSS, 순수 JavaScript로 구현하기 위한 설계서이다. 실제 코드는 포함하지 않으며, 개발 초보자가 기능을 한 단계씩 구현하고 확인할 수 있도록 구성한다.

### 확정된 개발 기준

- `game-plan.md`와 `game-design.md`는 `docs` 폴더로 이동하지 않고 프로젝트 루트에 둔다.
- 황금별은 사용자가 코딩 전에 직접 제작하여 제공하는 이미지 파일을 사용한다.
- 게임 종료 결과는 게임 영역 아래에 표시한다.
- Suno에서 생성한 배경음 `assets/sounds/background-music.mp3`를 게임 진행 중 재생한다.

## 1. 개발 방식과 제한 조건

### 개발 방식

- **HTML:** 제목, 점수, 남은 시간, 게임 영역, 버튼, 결과 메시지의 구조를 만든다.
- **CSS:** 밝은 파란색 계열 디자인, 황금별의 위치, PC·모바일 반응형 화면을 담당한다.
- **JavaScript:** 게임 상태, 타이머, 점수 계산, 별의 무작위 이동, 종료 및 재시작을 처리한다.
- **실행 방식:** 별도의 설치나 빌드 과정 없이 `index.html`을 브라우저에서 열어 실행한다.
- **배포 방식:** 모든 파일을 GitHub 저장소에 올린 뒤 GitHub Pages로 정적 배포한다.

### 제한 조건

- HTML, CSS, 순수 JavaScript만 사용한다.
- React, Vue, jQuery, 게임 엔진 등 외부 라이브러리를 사용하지 않는다.
- 서버, 데이터베이스, 회원가입, 로그인, 결제 기능을 만들지 않는다.
- 핵심 게임은 인터넷 연결 없이도 실행 가능하게 한다.
- 모든 파일 경로는 GitHub Pages에서 작동하도록 상대 경로를 사용한다.
- 황금별은 사용자가 제공하는 이미지 파일을 사용하며 이모지로 대체하지 않는다.
- 한 번의 플레이 시간은 30초, 목표 점수는 20점으로 고정한다.

## 2. 프로젝트 파일 및 폴더 구조

```text
golden-star-game/
├── index.html
├── css/
│   └── style.css
├── js/
│   └── game.js
├── assets/
│   ├── images/
│   │   ├── star.png
│   │   ├── background.jpg
│   │   └── trophy.png
│   └── sounds/
│       ├── background-music.mp3
│       ├── click.mp3
│       ├── success.mp3
│       └── game-over.mp3
├── game-plan.md
└── game-design.md
```

### 파일별 역할

| 파일·폴더 | 역할 | 초보자 설명 |
|---|---|---|
| `index.html` | 화면 요소 구성 | 사용자가 보는 제목, 숫자, 게임판, 버튼을 배치하는 파일이다. |
| `css/style.css` | 디자인과 반응형 처리 | 색상, 크기, 간격과 모바일 화면 모양을 정한다. |
| `js/game.js` | 게임 동작 처리 | 점수 증가, 시간 감소, 별 이동 등 실제 게임 규칙을 실행한다. |
| `assets/images/` | 이미지 보관 | 사용자가 제공하는 황금별 이미지와 선택 이미지를 모아 둔다. 황금별 이미지 때문에 MVP에서도 필요하다. |
| `assets/sounds/background-music.mp3` | 게임 배경음 | Suno에서 생성한 배경음 파일이다. `PLAYING` 상태에서 재생하고 게임이 끝나면 정지한다. |
| `assets/sounds/`의 나머지 파일 | 선택 효과음 보관 | 클릭음, 성공음, 종료음을 모아 둔다. 선택 효과음을 사용하지 않으면 해당 파일은 생략할 수 있다. |
| `game-plan.md` | 게임 기획 | 무엇을 만들 것인지 정리한 문서이다. |
| `game-design.md` | 구현 설계 | 기획을 어떤 구조와 순서로 만들지 정리한 문서이다. |

`game-plan.md`와 `game-design.md`는 프로젝트 루트에 유지한다. 황금별 이미지를 위한 `assets/images/` 폴더와 배경음을 위한 `assets/sounds/` 폴더도 유지한다. 선택 효과음을 사용하지 않으면 `click.mp3`, `success.mp3`, `game-over.mp3`는 생략할 수 있지만 `background-music.mp3`는 유지한다.

## 3. 화면 영역별 구성 요소

| 화면 영역 | 구성 요소 | 화면 역할 | 상태별 표시 |
|---|---|---|---|
| 전체 컨테이너 | 게임 전체를 감싸는 영역 | 콘텐츠 폭을 제한하고 화면 중앙에 배치한다. | 항상 표시 |
| 상단 영역 | 게임 제목, 한 줄 설명 | 게임의 목적과 방법을 알려준다. | 항상 표시 |
| 정보 영역 | 현재 점수, 남은 시간 | 플레이 상황을 큰 숫자로 보여준다. | 항상 표시 |
| 안내 영역 | 목표 점수 또는 상태 안내 | 시작 전 방법, 진행 중 목표, 종료 후 결과를 표시한다. | 상태에 따라 문구 변경 |
| 게임 영역 | 황금별이 움직이는 공간 | 별의 이동 범위를 제한한다. | 항상 표시 |
| 황금별 | 클릭 또는 터치 대상 | 선택하면 1점을 얻고 위치가 바뀐다. | `PLAYING`에서만 표시·활성화 |
| 결과 영역 | 성공·실패 문구, 최종 점수 | 게임 영역 바로 아래에서 게임 결과를 알려준다. | `GAME_OVER`에서 표시 |
| 버튼 영역 | 게임 시작 또는 다시 하기 버튼 | 시작 버튼은 하단에 표시하고, 다시 하기 버튼은 종료 결과와 함께 게임 영역 아래에 표시한다. | `READY`, `GAME_OVER`에서 표시 |

### 권장 화면 문구

- 제목: **황금별을 잡아라**
- 설명: **30초 동안 황금별을 빠르게 클릭해 보세요!**
- 시작 버튼: **게임 시작**
- 진행 안내: **목표 점수: 20점**
- 성공 메시지: **성공! 황금별 사냥꾼이 되었어요!**
- 실패 메시지: **아쉬워요! 다시 도전해 보세요.**
- 재시작 버튼: **다시 하기**

## 4. READY, PLAYING, GAME_OVER 게임 상태

게임 상태는 현재 어떤 동작을 허용할지 구분하는 값이다. 상태를 나누면 시작 전이나 종료 후에 별을 클릭해 점수가 오르는 오류를 막기 쉽다.

| 상태 | 의미 | 화면 모습 | 허용되는 입력 | 다음 상태 |
|---|---|---|---|---|
| `READY` | 게임 시작 전 | 점수 0, 시간 30초, 별 숨김, 시작 버튼 표시, 배경음 정지 | 게임 시작 버튼 | `PLAYING` |
| `PLAYING` | 게임 진행 중 | 점수와 시간 갱신, 별 표시, 시작 버튼 숨김, 배경음 재생 | 별 클릭·터치 | 시간이 0초가 되면 `GAME_OVER` |
| `GAME_OVER` | 게임 종료 후 | 별 숨김, 배경음 정지, 게임 영역 아래에 최종 결과와 다시 하기 버튼 표시 | 다시 하기 버튼 | `PLAYING` |

### 상태 전환 규칙

1. 페이지를 처음 열면 `READY` 상태로 시작한다.
2. 시작 버튼을 누르면 점수와 시간을 초기화하고 `PLAYING`으로 변경한다.
3. `PLAYING` 상태에서만 별 클릭과 타이머가 작동한다.
4. 남은 시간이 0이 되면 타이머를 멈추고 `GAME_OVER`로 변경한다.
5. 다시 하기 버튼을 누르면 게임 데이터를 초기화하고 바로 `PLAYING`으로 변경한다.

### 배경음 재생 규칙

- 파일 경로는 `assets/sounds/background-music.mp3`를 사용한다.
- 브라우저의 자동 재생 제한을 피하기 위해 페이지를 열자마자 재생하지 않고, 사용자가 **게임 시작** 또는 **다시 하기** 버튼을 누른 뒤 재생한다.
- `PLAYING` 상태에서만 배경음을 재생한다.
- 게임이 `GAME_OVER` 상태가 되면 배경음을 정지하고 재생 위치를 처음으로 되돌린다.
- 다시 하기 버튼을 누르면 배경음도 처음부터 다시 재생한다.
- 30초 동안 음악이 끊기지 않도록 필요하면 반복 재생한다.
- 배경음 음량은 별 클릭음과 결과 효과음을 방해하지 않도록 낮게 설정한다.

## 5. 저장해야 할 데이터

### 게임 실행 중 데이터

| 데이터 이름 | 자료 형태 | 시작값 | 용도 | 초보자 설명 |
|---|---|---:|---|---|
| 게임 상태 | 문자열 | `READY` | 현재 입력과 화면을 제어한다. | 시작 전, 진행 중, 종료 후를 구분하는 표지판이다. |
| 현재 점수 | 숫자 | `0` | 별 클릭 횟수를 기록한다. | 별을 누를 때마다 1씩 커지는 숫자이다. |
| 남은 시간 | 숫자 | `30` | 종료 시점을 판단한다. | 1초마다 1씩 줄어드는 숫자이다. |
| 제한 시간 | 숫자 상수 | `30` | 매 게임의 시작 시간을 정한다. | 실수로 값이 바뀌지 않도록 고정해서 사용한다. |
| 목표 점수 | 숫자 상수 | `20` | 성공과 실패를 판단한다. | 종료 시 현재 점수와 비교하는 기준이다. |
| 타이머 식별값 | 타이머 참조값 | 없음 | 실행 중인 타이머를 중지한다. | 타이머의 번호표이며, 중복 타이머를 막는 데 사용한다. |

### 브라우저 저장 데이터

MVP에서는 브라우저에 영구 저장해야 할 데이터가 없다. 최고 점수 기능을 추가할 때만 `localStorage`에 최고 점수 하나를 저장한다. 개인정보는 수집하거나 저장하지 않는다.

## 6. 사용자 입력과 처리 결과

| 사용자 입력 | 처리 가능 상태 | 처리 내용 | 화면 결과 |
|---|---|---|---|
| 게임 시작 버튼 클릭·터치 | `READY` | 점수 0, 시간 30초로 초기화하고 타이머를 시작한다. | 별이 나타나고 버튼이 숨겨진다. |
| 황금별 클릭·터치 | `PLAYING` | 점수를 1 증가시키고 별의 위치를 변경한다. | 점수 숫자가 바뀌고 별이 다른 곳에 나타난다. |
| 게임 영역의 빈 공간 클릭 | `PLAYING` | 아무 처리도 하지 않는다. | 점수와 별 위치가 유지된다. |
| 황금별 클릭·터치 | `READY` 또는 `GAME_OVER` | 입력을 무시한다. | 점수가 증가하지 않는다. |
| 다시 하기 버튼 클릭·터치 | `GAME_OVER` | 이전 타이머를 정리하고 데이터를 초기화한 뒤 새 게임을 시작한다. | 결과가 사라지고 별과 타이머가 다시 작동한다. |

게임 시작과 다시 하기 입력은 배경음 재생도 함께 시작한다. 시간이 0초가 되어 게임이 종료되면 배경음도 함께 정지한다.

모바일의 터치는 브라우저에서 기본 클릭 이벤트로 처리할 수 있으므로 MVP에서는 별도의 터치 이벤트를 만들지 않는다.

## 7. 필요한 기능 목록과 각 기능의 역할

### MVP 기능

| 번호 | 기능 | 역할 | 쉬운 설명 |
|---:|---|---|---|
| 1 | 화면 초기화 | 최초 점수, 시간, 문구와 버튼 상태를 설정한다. | 게임을 시작하기 전 화면을 정돈한다. |
| 2 | 게임 시작 | 데이터를 초기화하고 상태를 `PLAYING`으로 바꾸며 타이머를 실행한다. | 시작 버튼을 눌렀을 때 필요한 일을 한 번에 처리한다. |
| 3 | 타이머 갱신 | 1초마다 남은 시간을 줄이고 0초에서 게임을 끝낸다. | 게임의 30초 제한을 관리한다. |
| 4 | 별 무작위 배치 | 게임 영역의 실제 크기와 별 크기를 계산해 안전한 위치를 정한다. | 별이 게임판 밖으로 나가지 않게 옮긴다. |
| 5 | 별 클릭 처리 | `PLAYING` 여부를 확인하고 점수를 올린 뒤 별을 이동시킨다. | 별을 잡았을 때 발생하는 핵심 동작이다. |
| 6 | 게임 종료·재시작 | 타이머 중지, 승패 판정, 결과 표시와 새 게임 준비를 담당한다. | 시간이 끝난 뒤 결과를 보여주고 다시 시작하게 한다. |

### 선택 기능

| 기능 | 역할 | 적용 시점 |
|---|---|---|
| 최고 점수 저장 | 브라우저에 최고 기록을 보관한다. | MVP 완료 후 |
| 효과음 재생 | 클릭과 종료 순간을 소리로 알려준다. | MVP 완료 후 |
| 반짝임 애니메이션 | 클릭 성공을 시각적으로 강조한다. | MVP 완료 후 |
| 5초 경고 표시 | 시간이 얼마 남지 않았음을 색상으로 알린다. | MVP 완료 후 |

### 확정된 배경음 기능

| 기능 | 역할 | 쉬운 설명 |
|---|---|---|
| 배경음 재생·정지 | 게임 시작 시 `background-music.mp3`를 재생하고 게임 종료 시 정지한다. | 게임이 진행되는 동안만 음악이 들리도록 관리한다. |

## 8. 오류를 예방하기 위한 예외 상황

| 예외 상황 | 발생할 수 있는 문제 | 예방 방법 |
|---|---|---|
| 시작 버튼을 여러 번 빠르게 누름 | 타이머가 여러 개 실행되어 시간이 빨리 줄어든다. | 게임 시작 전에 기존 타이머가 있으면 반드시 중지하고, `PLAYING`에서는 시작 입력을 받지 않는다. |
| 시간이 끝나는 순간 별을 클릭함 | 종료 후에도 점수가 1점 증가할 수 있다. | 별 클릭 처리의 첫 단계에서 상태가 `PLAYING`인지 확인한다. |
| 남은 시간이 0보다 작아짐 | 화면에 `-1초`가 표시된다. | 시간을 감소시킨 뒤 0 이하이면 값을 0으로 고정하고 즉시 종료한다. |
| 별이 게임 영역 밖에 배치됨 | 별 일부가 잘리거나 클릭할 수 없다. | 게임 영역 크기에서 별의 가로·세로 크기를 뺀 범위 안에서 좌표를 만든다. |
| 모바일 화면 크기가 변경됨 | 기존 별 위치가 새 게임 영역 밖으로 나갈 수 있다. | 화면 크기 변경 시 별을 다시 안전한 위치로 배치한다. |
| 버튼을 빠르게 반복 클릭함 | 게임이 중복 초기화된다. | 상태를 먼저 변경한 뒤 버튼을 숨기거나 비활성화한다. |
| 황금별 이미지 파일이 없거나 경로가 잘못됨 | 클릭 대상인 별이 보이지 않는다. | 개발 전에 제공된 이미지의 파일명과 저장 경로를 확인하고 GitHub Pages에서 작동하는 상대 경로를 사용한다. 효과음 실패는 게임 진행을 막지 않게 한다. |
| 배경음 자동 재생이 차단됨 | 페이지를 열었을 때 음악이 재생되지 않는다. | 페이지 로드 시 자동 재생하지 않고 사용자가 게임 시작 버튼을 누른 뒤 재생한다. |
| 다시 시작할 때 배경음이 겹침 | 같은 음악이 중복 재생된다. | 새로 재생하기 전에 기존 재생을 정지하고 재생 위치를 처음으로 되돌린다. |
| 배경음 파일이 없거나 경로가 잘못됨 | 음악 재생 오류가 발생한다. | `assets/sounds/background-music.mp3`의 파일명, 대소문자와 상대 경로를 확인하며 음악 오류가 게임 진행을 중단시키지 않게 한다. |
| 게임 영역의 크기가 0임 | 무작위 좌표 계산 결과가 잘못된다. | 화면이 표시된 후 크기를 계산하고, 크기가 유효하지 않으면 별을 중앙에 둔다. |
| 브라우저 탭이 백그라운드로 이동함 | 타이머 표시가 실제 경과 시간과 조금 달라질 수 있다. | MVP에서는 허용하되, 확장 단계에서는 종료 시각을 기준으로 남은 시간을 다시 계산한다. |

## 9. PC와 모바일 반응형 조건

### 공통 조건

- 전체 콘텐츠는 화면 중앙에 배치하고 최대 너비를 제한한다.
- 점수와 남은 시간은 같은 크기와 스타일로 표시한다.
- 버튼과 별은 손가락으로도 누르기 쉽도록 충분한 클릭 영역을 제공한다.
- 게임 영역 밖으로 별이 이동하지 않아야 한다.
- 가로 스크롤이 생기지 않아야 한다.

### PC 화면

- 기준 화면 너비: 768px 이상
- 게임 영역은 최대 너비 약 800px 안에서 넓게 표시한다.
- 점수와 남은 시간을 가로 한 줄에 나란히 배치한다.
- 게임 영역 높이는 약 420~500px 범위로 구성한다.

### 모바일 화면

- 기준 화면 너비: 767px 이하
- 좌우 여백을 줄이고 게임 영역은 화면 너비에 맞춘다.
- 점수와 남은 시간은 가능한 한 가로로 유지하되, 매우 좁으면 두 칸의 너비를 동일하게 줄인다.
- 게임 영역 높이는 화면 크기에 따라 약 320~420px로 조정한다.
- 버튼의 높이는 최소 44px, 황금별의 클릭 영역도 최소 44×44px 이상으로 한다.
- 글자가 너무 작아지지 않도록 본문은 약 16px 이상을 권장한다.
- 모바일에서 두 번 탭할 때 화면이 확대되거나 텍스트가 선택되어 플레이를 방해하지 않도록 게임 영역의 기본 터치 동작을 조정한다.

## 10. 기능 구현 순서

1. **프로젝트 폴더 만들기**  
   `index.html`, `css/style.css`, `js/game.js`를 만들고 서로 연결한다.

2. **HTML 화면 구조 만들기**  
   제목, 설명, 점수, 시간, 게임 영역, 별을 배치하고 게임 영역 아래에 결과 문구와 다시 하기 버튼 영역을 배치한다.

3. **기본 CSS 디자인 적용하기**  
   파란색 계열 색상, 글자 크기, 게임 영역 크기와 버튼 모양을 정한다.

4. **게임 데이터와 상태 정의하기**  
   점수, 시간, 목표 점수, 타이머 식별값, `READY` 상태를 준비한다.

5. **게임 시작 기능 만들기**  
   시작 버튼으로 데이터를 초기화하고 `PLAYING` 상태로 전환한다.

6. **별 무작위 배치 기능 만들기**  
   별이 게임 영역 안에서만 새로운 위치로 이동하게 한다.

7. **별 클릭과 점수 기능 만들기**  
   별을 누르면 점수가 1 증가하고 별이 즉시 이동하게 한다.

8. **30초 타이머 만들기**  
   남은 시간을 1초마다 줄이고 0초에 종료 기능을 호출한다.

9. **종료와 재시작 기능 만들기**  
   승패를 판정하고 게임 영역 아래에 결과와 다시 하기 버튼을 표시한다.

10. **배경음 재생과 정지 기능 만들기**  
    게임 시작과 다시 하기에서 `background-music.mp3`를 처음부터 재생하고, 게임 종료 시 정지한다.

11. **반응형 화면 적용하기**  
    PC와 모바일에서 글자, 버튼, 게임 영역을 확인하고 조정한다.

12. **예외 상황 테스트하기**  
    빠른 연속 클릭, 종료 순간 클릭, 화면 크기 변경 등을 확인한다.

13. **GitHub Pages 배포 확인하기**  
    파일의 대소문자와 상대 경로를 확인하고 실제 배포 주소에서 실행한다.

## 11. 기능별 완료 조건

| 기능 | 완료 조건 | 확인 방법 |
|---|---|---|
| 파일 연결 | HTML을 열었을 때 CSS 디자인이 적용되고 JavaScript 오류가 없다. | 브라우저 화면과 개발자 도구 콘솔을 확인한다. |
| 초기 화면 | 점수 0, 남은 시간 30초, 시작 버튼이 보이고 별은 비활성 상태이다. | 페이지를 새로 고친다. |
| 게임 시작 | 시작 버튼을 한 번 누르면 버튼이 숨겨지고 별과 타이머가 작동한다. | 시작 버튼을 클릭한다. |
| 타이머 | 30부터 0까지 1초 단위로 감소하고 음수가 표시되지 않는다. | 한 게임을 끝까지 진행한다. |
| 별 배치 | 별 전체가 게임 영역 안에 표시되고 여러 위치로 이동한다. | 별을 10회 이상 클릭하고 화면 가장자리를 확인한다. |
| 점수 계산 | 별을 한 번 누를 때마다 정확히 1점 증가하고 빈 공간 클릭은 반영되지 않는다. | 별과 빈 공간을 번갈아 클릭한다. |
| 상태 제어 | `PLAYING` 상태에서만 별 클릭으로 점수가 증가한다. | 시작 전과 종료 후 별 입력을 시도한다. |
| 게임 종료 | 0초가 되면 타이머와 별 입력이 멈추고 게임 영역 아래에 최종 점수가 표시된다. | 제한 시간이 끝날 때까지 기다린다. |
| 승패 판정 | 20점 이상은 성공, 20점 미만은 실패 메시지가 표시된다. | 점수가 다른 두 번의 게임으로 확인한다. |
| 재시작 | 다시 하기 버튼을 누르면 점수 0, 시간 30초로 새 게임이 시작된다. | 게임 종료 후 다시 하기 버튼을 누른다. |
| 배경음 | 게임 시작과 다시 하기에서 음악이 처음부터 재생되고, 0초가 되면 정지하며 중복 재생되지 않는다. | 시작, 종료, 다시 하기 순서로 실행하고 소리를 확인한다. |
| 배경음 파일 경로 | 로컬 실행과 GitHub Pages에서 `background-music.mp3`가 정상적으로 로드된다. | 두 환경에서 네트워크 오류와 브라우저 콘솔을 확인한다. |
| 반응형 화면 | PC와 모바일에서 가로 스크롤, 잘림, 겹침 없이 조작할 수 있다. | 브라우저 창 크기와 모바일 미리보기를 사용한다. |
| GitHub Pages 실행 | 배포 주소에서 새로 고침해도 화면과 게임 기능이 정상 작동한다. | GitHub Pages 주소로 직접 접속한다. |

## 최종 설계 확인 사항

- 핵심 기능은 `game-plan.md`에서 정한 6개 MVP 범위를 유지한다.
- 게임의 기본 흐름은 `READY → PLAYING → GAME_OVER` 순서로 동작한다.
- 모든 사용자 입력은 현재 게임 상태를 확인한 뒤 처리한다.
- 사용자가 제공한 황금별 이미지와 `assets/sounds/background-music.mp3`를 사용한다. 선택 효과음은 없어도 핵심 게임을 플레이할 수 있다.
- 문서는 프로젝트 루트에 유지하며, 종료 결과는 게임 영역 아래에 표시한다.
